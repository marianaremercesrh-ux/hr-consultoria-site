import{Copy,Linkedin,MessageCircle,QrCode}from"lucide-react";import{adminButtonClass}from"./AdminUI";
export default function JobShareActions({slug,title}:{slug:string;title:string}){const url=`${window.location.origin}/vagas/${slug}`;async function copy(){await navigator.clipboard.writeText(url)}return <div className="flex flex-wrap gap-2"><button type="button" onClick={()=>void copy()} className={adminButtonClass('secondary')} title="Copiar link"><Copy size={15}/></button><a href={`https://wa.me/?text=${encodeURIComponent(`${title} ${url}`)}`} target="_blank" rel="noreferrer" className={adminButtonClass('success')} title="Compartilhar no WhatsApp"><MessageCircle size={15}/></a><a href={`https://www.linkedin.com/sharing/share-offsite/?url=${encodeURIComponent(url)}`} target="_blank" rel="noreferrer" className={adminButtonClass('secondary')} title="Compartilhar no LinkedIn"><Linkedin size={15}/></a><a href={`https://api.qrserver.com/v1/create-qr-code/?size=300x300&data=${encodeURIComponent(url)}`} target="_blank" rel="noreferrer" className={adminButtonClass('secondary')} title="Abrir QR Code"><QrCode size={15}/></a></div>}
