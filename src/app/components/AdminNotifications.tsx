import{useEffect,useState}from"react";import{Bell}from"lucide-react";import{listEntrevistas}from"../services/ats";import type{Entrevista}from"../types/ats";
export default function AdminNotifications(){const[open,setOpen]=useState(false),[items,setItems]=useState<Entrevista[]>([]);useEffect(()=>{void listEntrevistas().then(setItems).catch(()=>undefined)},[]);const today=new Date().toISOString().slice(0,10),count=items.filter(x=>x.data===today&&!["cancelada","realizada"].includes(x.status)).length;return <div className="relative"><button type="button" onClick={()=>setOpen(!open)} aria-label="Notificações" aria-expanded={open} className="relative flex h-10 w-10 items-center justify-center border border-white/20 text-white transition hover:border-[#D4A62A] hover:text-[#D4A62A] focus:outline-none focus:ring-2 focus:ring-[#D4A62A]"><Bell size={19}/>{count>0&&<span className="absolute -right-1 -top-1 rounded-full bg-[#D4A62A] px-1.5 text-xs text-[#052656]">{count}</span>}</button>{open&&<div className="absolute right-0 z-50 mt-2 w-[min(88vw,320px)] bg-white p-5 text-[#052656] shadow-xl"><strong>Entrevistas de hoje</strong><p className="mt-2 text-sm text-gray-600">{count?`${count} entrevista(s) aguardando acompanhamento.`:"Nenhuma entrevista pendente hoje."}</p><a href="/admin/agenda" className="mt-3 inline-block font-semibold underline">Abrir agenda</a></div>}</div>}
